#!/bin/bash
set -e -x

mkdir server
tar zxvf kubernetes/kubernetes-server-linux-amd64.tar.gz -C server

mkdir -p ${BOSH_INSTALL_TARGET}/bin
cp server/kubernetes/server/bin/hyperkube ${BOSH_INSTALL_TARGET}/bin
